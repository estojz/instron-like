<Window x:Class="InstronLike.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InstronLike" Height="350" Width="500">

   <Window x:Class="InstronLike.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        Title="InstronLike" Height="450" Width="700">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Start" Width="120" Click="Start_Click"/>
            <Button Content="Stop" Width="120" Margin="10,0,0,0" Click="Stop_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Name="ForceText" FontSize="16"/>
            <TextBlock Name="PositionText" FontSize="16"/>
            <TextBlock Name="TimeText" FontSize="16"/>
        </StackPanel>

        <scottplot:WpfPlot x:Name="Plot" Grid.Row="2"/>

    </Grid>
</Window>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Start" Width="120" Click="Start_Click"/>
            <Button Content="Stop" Width="120" Margin="10,0,0,0" Click="Stop_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Name="ForceText" FontSize="16"/>
            <TextBlock Name="PositionText" FontSize="16"/>
            <TextBlock Name="TimeText" FontSize="16"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Margin="0,10,0,0">
            <TextBlock Text="Graph will be here (next step)" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

    </Grid>
</Window>
